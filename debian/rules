#!/usr/bin/make -f

# output every command that modifies files on the build system.
#export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_build:
	install --mode=755 pkispawn.sh pkispawn

override_dh_auto_clean:
	rm --force pkispawn

override_dh_auto_test:
	for i in *.sh; do bash -n $$i; done
